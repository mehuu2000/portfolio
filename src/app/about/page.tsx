import SelfIntroductionSection from '@/components/sections/SelfIntroductionSection';

export const runtime = "edge";

export default function About() {
  return (
    <main>
      <SelfIntroductionSection />
    </main>
  );
}